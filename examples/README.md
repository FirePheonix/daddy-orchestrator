# Examples

This directory collects minimal workflow examples for the `daddy` CLI.

Files:

- `daddy.json`: reusable project defaults
- `mcp-servers.json`: sample MCP server definitions
- `one-shot.md`: single-prompt usage
- `chat-and-resume.md`: multi-turn chat and resume flows
- `inspect-and-view.md`: trajectory inspection and local viewer usage
